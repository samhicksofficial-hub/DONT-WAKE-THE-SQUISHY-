# place/

The saved Studio place file lives here.

In Studio: **File -> Save to File As...** and save `DontWakeTheSquishy.rbxl`
into this folder. `.gitignore` allows `place/*.rbxl` through so the hand-built
map and imported models are version-controlled, while build output elsewhere
in the repo stays ignored.
